# NORA: Neighbour-Oriented Reward Anchoring

Code for **"The Variance of a Judge: Zero-Variance Rewards by Deterministic Regression"**, accepted at the **ACL 2026 Workshop on Uncertainty-Aware NLP (UncertaiNLP)**.

```bash
python code/main.py --k 16 --epochs 3 --seed 42
```
